# Backend (Node + Architecture en couches)

## Lancer

1. `npm install`
2. `npm run prisma:generate`
3. `npm run prisma:migrate`
4. (Optionnel) `npm run prisma:seed`
5. `npm run dev`

API: `http://localhost:4000/api`
Health: `GET /api/health`
